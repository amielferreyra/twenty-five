import React from 'react'
import { Formulario } from '../components/formulario'

function Contact(){
    return(
        <section class="fondo">
            <Formulario/>
        </section>
    );
}

export default Contact;